import { getEmbedInfo } from '$lib/server/PowerBI/powerbi-service.js'
import { env } from '$env/dynamic/private'

export const load = async ({ url, params, request, route, fetch, locals }) => {
 
    return { message : 'starter kit' }

}